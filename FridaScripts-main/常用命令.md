```shell
pm list package|grep hong
pm path com.sinyee.babybus.recommendapp

cat >t.js <<ENDFILE

Interceptor.attach(Module.findExportByName(null, 'android_dlopen_ext'), {
    onEnter: function(args) {
        var library_path = Memory.readCString(args[0])
        if (library_path) { 
            console.log(library_path)
        }
    }

})

ENDFILE

```

cat -n 显示行号

dumpso

```js
libSo = Process.getModuleByName(soName);
```

package:com.sinyee.babybus.recommendapp
xun:/ # pm list package|grep hong
package:com.hongen.app.wordim

File.writeAllText(`${Process.getTmpDir()}/exps.txt`,JSON.stringify(exps))

m = Process.enumerateModules().filter(v=>/libcocos2d.*/i.test(v.name))[0]
exps = m.enumerateExports()
exps.filter(v=>/filedata/i.test(v.name))


m = Process.findModuleByName("libcocos2dlua.so")

m.base.add(0x2225228).readUtf8String()
